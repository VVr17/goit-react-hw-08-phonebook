import { LoginForm } from 'components/LoginForm/LoginForm';
import { Section } from 'components/Section/Section';

export const Login = () => {
  return (
    <Section title="Login">
      <LoginForm />
    </Section>
  );
};
